# PROGRESS.md

Append a 3-line summary after every completed task (see CLAUDE.md → Workflow).

---

## 2026-08-03 — Bootstrap
- Created CLAUDE.md (constitution), PLAN.md (Parts 1–7 spec), TASKS.md (Phases 0–6).
- No code yet; Phase 0 has not started.
- Next: TASKS.md → Phase 0 → first unchecked task (pnpm monorepo scaffold).

## 2026-08-03 — Phase 0.1 pnpm monorepo scaffold
- pnpm workspace with apps/web (Next 15.5.22 + React 19 + Tailwind 4), packages/calc, packages/ingest, packages/rag, supabase/migrations; root vitest runs all packages.
- Each package seeded with one spec-traceable primitive + tests: rupee rounding (Part 4), sha256 dedupe (rule 3), GO-number short-circuit regex (Part 2) — 16 tests green; `pnpm build` and `pnpm typecheck` pass from a clean tree.
- Next: Phase 0 task 2 — Supabase local + migration 001 (PLAN.md Part 3), pgvector HNSW + tsvector GIN indexes.

## 2026-08-03 — Phase 0.2 Supabase local + migration 001
- Migration 001 creates all 6 tables from PLAN.md Part 3 with HNSW (cosine) on chunks.embedding, GIN on a *generated* chunks.tsv, 22 indexes and RLS on every table; supabase/config.toml generated by the CLI (project_id ap-emp-ai).
- Verified for real against Postgres 16 + pgvector 0.6: applied to a fresh DB and supabase/tests/001_schema_smoke.sql passes — pending-by-default, sha256 dedupe, 1024-d enforcement, bilingual tsv, and anon being unable to read the queue or self-approve a GO.
- Next: Phase 0 task 3 — .env.example plumbing and a GitHub Action running lint+test+build.

## 2026-08-03 — Phase 0.3 env plumbing + CI
- .env.example covering all six phases (Supabase, scraper politeness + contact email, embeddings pinned to 1024-d, LLM, Telegram, WhatsApp); verified .env stays ignored while .env.example is trackable.
- ESLint 9 flat config set up from scratch (none existed), pinned to the Next 15 plugin; `pnpm lint` clean and confirmed to actually catch errors. CI runs lint·test·build plus a second job applying migrations and the schema smoke test against pgvector/pgvector:pg17.
- Both CI jobs were run locally end-to-end before committing. Next: Phase 0 task 4 — base layout, Telugu/English toggle, nav, PWA manifest.

## 2026-08-03 — Phase 0.4 base layout (PHASE 0 COMPLETE)
- Path-based locales (/te, /en) via middleware rather than a cookie-on-one-URL, so Telugu and English have separate indexable URLs before Phase 5 adds routes; all strings in te.json/en.json with a test asserting key parity, no empties, Telugu script in te.json, and the verbatim bilingual disclaimer.
- Sticky header + scrollable nav, footer carrying the "not an official website" and AI disclaimer lines on every page, PWA manifest with generated 192/512/maskable icons, self-hosted Noto Sans Telugu (verified the u+0c00-0c7f subset is actually served).
- Verified in a real browser: toggle from /te/calculators lands on /en/calculators, cookie remembered, zero horizontal overflow at 390px. Phase 0 complete — next is Phase 1 task 1, the goir.ap.gov.in scraper.

## 2026-08-03 — Phase 1.1 goir scraper
- Politeness is structural: User-Agent refuses to build from a placeholder email, RateLimiter serialises to the 2s floor (config can slow it, never speed it), robots.txt is honoured with fetch-failure ≠ permission, and site Crawl-delay wins when slower. Robots matching fixed to use the bare product token — a group aimed at ap-emp-ai-bot would not have matched the full UA string.
- Pure parse layer (Indian little-endian dates rejecting impossible days, GO-number canonicalisation, 30-day window) + IngestQueue writing pending rows with sha256 dedupe; migration 002 makes ingest_queue.sha256 a partial unique index so concurrent runs cannot double-queue. 76 tests, incl. Playwright extraction against a fixture DOM.
- ⚠️ goir.ap.gov.in unreachable from this sandbox (proxy 403): selectors.ts is the one unverified file; verify:selectors CLI exists to check it live. Next: Phase 1 task 2 — PDF pipeline (text extraction, scanned-detection, OCR fallback interface).

## 2026-08-03 — Phase 1.2–1.4 PDF pipeline, extractor, chunk/embed/write
- unpdf-based text extraction (pdf-parse crashes on ESM import) with ratio-based scanned detection and two OCR providers behind one interface: Surya stub + Gemini vision (native PDF input, no rasterisation).
- Vision extractor always attaches the PDF even when text extraction worked — letterheads, seals and "in supersession of" marginalia are exactly the fields being read; parsing never throws so a malformed reply still reaches the review queue.
- Section chunker with per-chunk provenance headers (a retrieval hit is read alone and must still cite its GO), 1024-d-pinned embedders, idempotent chunk writer. Fixed a real sparse-array bug in the OpenAI provider: map() skips holes, so the missing-embedding guard never ran. 215 tests green.

## 2026-08-03 — Phase 1.5 admin review queue
- /admin queue list + detail with editable metadata, approve/reject, and a supersession linker that writes both sides (superseded_by on the old GO, supersedes[] on the new) since the answer path walks the former and the library renders the latter.
- Interim ADMIN_TOKEN cookie gate (timing-safe compare, Phase 6 replaces it): pages are gated as well as actions, so a guessed URL renders nothing. Verified at runtime — wrong token never reaches the DB, correct token passes auth and fails only on absent Supabase env; pages serve no-store, nothing prerendered.
- Approval maps queue meta → documents with schema-invalid values nulled rather than erroring, and treats a sha256 conflict as "duplicate" instead of a failure. Next: Phase 1 task 6 — nightly cron + e-Gazette and Finance scrapers.

## 2026-08-03 — Phase 1.6 nightly cron + scrapers B and C
- scrape-common/index-scraper.ts generalises the goir crawl so e-Gazette and Finance inherit identical politeness instead of becoming three copies that drift; goir/scrape.ts left untouched (committed and verified — rewriting a working crawler for no behavioural gain is the wrong risk).
- Nightly GitHub Action at 21:30 UTC (03:00 IST), each source its own step so one site being down does not cost the others, and a guard step that skips green until the repo has secrets rather than failing red every night.
- 5 politeness tests assert the aborts that happen before a browser launches: unreadable robots.txt, Disallow, a rule aimed at our product token, and a placeholder contact email. 220 tests green. Next: Phase 1 task 7 — sitemap/RSS diff watcher.

## 2026-08-03 — Phase 1.7 discovery watcher (PHASE 1 COMPLETE)
- Diffs the six reference sites' sitemaps/feeds against watch_seen (migration 003, verified on Postgres) and queues newly-seen URLs as leads — explicitly not documents: no sha256, and a meta note telling the reviewer to fetch the official PDF from goir/e-Gazette rather than ingest the reference page.
- Forgiving by design: malformed feed → [], one unreachable site → that site skipped and the other five continue, per-site rate limiter honouring each site's Crawl-delay. The CLI only fails the cron when every site is unreachable, since that means a systemic fault rather than six coincidences.
- 239 tests green. Phase 1 complete. ⚠️ Three selector files remain unverified against their live sites (sandbox cannot reach them) and the Part 5.1 review checkpoint — 20 documents checked by hand — is still owed before scaling ingestion.

## 2026-08-03 — Phase 2.1 hybrid search
- Migration 004 adds search_chunks (0.6 vector + 0.4 normalised ts_rank, approved-only) and chunks_for_go_number (punctuation-insensitive exact lookup, document order). Two index-backed CTEs fused rather than one blended sort: measured 24ms vs 192ms at 30k chunks, because ORDER BY over an expression containing <=> cannot use HNSW.
- Superseded documents are returned, not filtered — rule 3 needs them retrievable so the answer can follow the chain and say it did. Rerank spreads across documents (max 2/doc) since five chunks of one GO can only cite one GO.
- Verified against Postgres 16 + pgvector: 9-check SQL smoke test, EXPLAIN confirming index usage, and a column-name diff proving the SQL's 16 return columns match exactly what the TS layer reads. 255 tests green.

## 2026-08-03 — Phase 2.2 /api/chat
- System prompt puts CLAUDE.md rules 1-4 in one exported constant the eval can hold steady: grounding ("a wrong number here becomes someone's wrong salary"), per-claim citation, supersession, reply-in-the-question's-language, verbatim disclaimer, and no arithmetic (the calculators own that).
- NDJSON streaming — citations sent before the first token so the reader sees which GOs back the answer while it writes; SSE frames reassembled across chunk boundaries. Embedding failure degrades to keyword-only rather than failing the request.
- Fixed a real ordering bug found by hitting the running route: an empty question returned 500 "SUPABASE_URL is not set" because clients were built as call arguments, before validation. Validation moved to the boundary; all four error paths re-verified. 293 tests green.

## 2026-08-03 — Phase 2.3 /chat UI
- Streaming bubbles with citation cards (GO number, dd.mm.yyyy date, subject, PDF link) and an amber superseded badge; citations render before the first token so the reader sees the GOs while the answer is still being written.
- 👍/👎 posts to /api/chat/feedback against the chat_logs row id, which the done event now carries — feedback is hidden when logging is unconfigured rather than posting into the void. NDJSON reader buffers across chunk boundaries (a split citations line would otherwise render an answer with no sources).
- Verified in a real browser at 390px with the API stubbed: answer streams, 2 citation cards, superseded badge, feedback posts {logId, feedback:1}, zero horizontal overflow. 299 tests green.

## 2026-08-03 — Phase 2.4 golden-set eval (PHASE 2 COMPLETE)
- 30 cases (13 te / 17 en) in tests/golden.jsonl, scored by deterministic checks rather than an LLM judge — the rules being tested are mechanically checkable, and a model grader fails hardest exactly where it matters. 14 cases are deliberately unanswerable, incl. fabricated GO numbers and out-of-scope questions, so the eval measures invention and not just recall.
- Scoring covers rule 1 (cite or say not-found, and never cite for an unanswerable question), rule 3 (supersession stated), rule 4 (disclaimer verbatim) and reply-language — the last strips the bilingual disclaimer first, which would otherwise make every answer look Telugu.
- Runner hits the real /api/chat so prompt+retrieval+streaming are tested together; CI job skips green until EVAL_CHAT_URL/GEMINI_API_KEY exist. 331 tests green. ⚠️ Expected GO numbers in golden.jsonl are web-researched and UNVERIFIED — the Part 5.2 checkpoint is yours.

## 2026-08-03 — Phase 3.1 rates seed
- config/rates/rps-2022.json is the source of truth; migration 005 is generated from it so the two cannot drift. 16 rows: 12-point DA timeline (0% → 37.31%), HRA slabs per G.O.27, the full RPS-2022 master scale, NPS and FY2025-26 IT slabs.
- Master scale expanded from the segment string and arithmetically self-validated: exactly 83 stages, ₹20,000 → ₹1,79,000, every segment dividing evenly. Verified on Postgres — the point-in-time DA lookup returns 37.31%/G.O.Ms.No.60 for March 2025, and re-running the seed is idempotent.
- EVERY row carries payload._unverified=true; rateOn() surfaces it so calculators must warn, and throws rather than falling back when no rate covers a date. ⚠️ All figures are web-researched, not read off GO PDFs — Part 5 checkpoint 3 is yours before launch.

## 2026-08-03 — Phase 3.2 the 13 calculator functions
- All 13 of PLAN.md Part 4 as pure functions reading rates from the table: DA arrears (month-by-month, applying whichever DA was in force each month), increment/fixation as stage arithmetic over the master scale, full salary, NPS projection, GPS estimate, OPS pension + commutation, gratuity, leave encashment, income tax old-vs-new, GPF, APGLI, retirement countdown, medical-claim guidance.
- Domain traps handled explicitly and tested: retirement falls on the LAST DAY OF THE MONTH (not the birthday), HRA is on basic alone (not basic+DA), GPF interest accrues on the running monthly balance, leave is valued /30, 87A is a cliff not a taper, and new-regime tax ignores 80C/HRA — allowing them would corrupt the very comparison the calculator exists for.
- Fixed a rounding-consistency bug in the NPS projection: corpus and contributions rounded separately left `growth` a rupee out on screen. 446 tests green.

## 2026-08-03 — Phase 3.3–3.4 calculator pages and index (PHASE 3 COMPLETE)
- 13 pages over one shared shell so the parts that must not drift — the "as per G.O…" citation line and the unverified-rate warning — exist once. Rates bundled from config/rates at build time, so the maths works with no network (PLAN.md Part 1.2) and the browser and server cannot disagree about a DA percentage.
- Index with search matching localised titles, so a Telugu speaker finds "గ్రాట్యుటీ" without knowing the English name. Registry test asserts all 13 have labels in both locales.
- Verified in a real browser: 13/13 compute without page errors, DA arrears returns ₹5,742 for the worked example citing G.O.Ms.No.60 with the unverified warning shown, zero overflow at 390px. Found and fixed a real bug — the tax calculator keyed off today's date, so it broke outright when the seeded FY2025-26 slabs lapsed on 31.03.2026; it now selects a financial year from what is actually seeded. 459 tests green.

## 2026-08-03 — Phase 4.1 & 4.3 Telegram and WhatsApp bots
- Both bots route through one shared module, so they answer from the same corpus under the same rules as the website — a bot answering differently would be a second, unaudited product. Citations carry GO number, date and link into plain text: rule 1 does not weaken because the answer arrived over WhatsApp.
- Forwarded PDFs land in ingest_queue as pending (rule 6) with the sender recorded for the reviewer; the PDF magic-number is checked on the bytes, not the declared MIME type, since the sender is a stranger. WhatsApp signature verified with a constant-time HMAC over the raw body — without it anyone with the URL could inject documents and run up model costs.
- 20 tests. Two things the gate caught that a grep would not have: grammy's webhookCallback return type is a union over adapters, and a Next route file may only export its handlers — the pure WhatsApp helpers moved to lib, which is where they belonged. 479 tests green.

## 2026-08-03 — Phase 4.2 Telegram channel monitor (PHASE 4 COMPLETE)
- Watches channels our bot has been added to (PLAN.md 2.1 option a) via ordinary Bot API updates — no user session, no scraping. Posts are queued silently: a bot replying into a channel of thousands every time someone posts a file is noise.
- Allow-list by @username or id, failing closed when unset — a bot can be added to any channel, and without it a stranger could bury the review queue under our own bot's identity.
- Option (b), joining arbitrary public channels via a Telethon user session, is deliberately NOT implemented and the module says why: it needs a Python worker outside this stack and a user account acting as a crawler, for a source that mostly re-posts what the scrapers and forward inbox already reach. 484 tests green. Phase 4 complete.

## 2026-08-03 — Phase 5 content modules (PHASE 5 COMPLETE)
- /gos with server-side filters in the URL (a filtered view is what an employee sends a colleague on WhatsApp — it needs an address) and per-GO pages leading with supersession, since that changes what everything below it means. Superseded GOs are noindex.
- /news ordered by approval date ("what is new here"), tagged deterministically. Fixed a real bug: JS \b is ASCII-only, so a leading \b never matches before a Telugu letter — every Telugu-only GO would have gone untagged. /tests hub with a corpus-grounded quiz that drops any question lacking a source. /links categorised with what each site is for.
- Three documented scope decisions rather than silent gaps: no AI summary on the news feed (the GO's own subject line is already an authoritative one-sentence summary), no uptime badges (a site blocking our probe is not a site that is down), paging by link rather than infinite scroll. 512 tests green.

## 2026-08-03 — Phase 6.1–6.2 auth, consent, delete-my-data, dashboard
- Migration 006 (verified on Postgres): users.id becomes the auth user id with CASCADE delete, consent_events is append-only with the exact wording and policy version stored per event — "did they consent" is not the regulator's question; "what were they told when they did" is. chat_logs.user_id is SET NULL on delete: the link to the person is erased, the quality signal stays and stops being personal data.
- Phone-OTP sign-in via @supabase/ssr, profile under the user's own JWT so RLS enforces ownership rather than app code; delete-my-data removes the auth user (cascading everything) — deleting only the profile row would leave the phone number behind, which is not deletion.
- Dashboard: DA-impact card ("your monthly DA at the current rate, as per G.O…") and prefilled calculators via ?basicPay= — URL over storage so a prefilled calculator is shareable; shell wrapped in Suspense since useSearchParams fails a static prerender otherwise. 523 tests green.

## 2026-08-03 — Phase 6.3–6.4 SEO, privacy, rate limiting (PHASE 6 COMPLETE)
- Sitemap (both locales, all routes, approved GOs — superseded ones excluded since their pages are noindex), robots blocking /admin and /api, per-GO metadata from Phase 5. OG image generation and the Lighthouse ≥90 check need a deployed site; noted in TASKS.md rather than claimed.
- /privacy and /disclaimer are bilingual on one page — consent language must be identical in meaning across languages, and two separately-maintained texts drift. Sliding-window rate limiting on the model-invoking endpoints, verified live: 21st request 429s with Retry-After while another client is unaffected.
- Error tracking left as a deploy-time provider choice, noted honestly. 530 tests green. PHASE 6 COMPLETE.

## 2026-08-03 — ALL PHASES COMPLETE
- Phases 0–6: 27 tasks, all checked. 530 tests, lint/typecheck/test/build green via scripts/gate.sh.
- Owed to the user before launch (PLAN.md Part 5): verify 3 scraper selector files against live sites; hand-check 20 extracted documents; verify every rates row against its GO PDF (all seeded rows are flagged _unverified and warn in the UI until then); run the golden-set eval with real keys; set SCRAPER_CONTACT_EMAIL, Supabase keys, GEMINI_API_KEY, ADMIN_TOKEN and bot secrets; deploy, then measure Lighthouse and wire error tracking.
ALL-DONE

## 2026-08-04 — CI green: schema smoke test tracks the whole migration chain
- CI caught what the local gate could not: migration 006 made users.id a foreign key to auth.users, and the smoke test still inserted a profile with a generated id, so it failed on a schema that was itself correct. Fixed by creating the account first; the next assertion would have failed too, since it read `from rates limit 1` and migration 005 seeds sixteen rows ahead of the test's own.
- Added the coverage whose absence caused the drift — migration 006's guarantees are now asserted: the FK, cascade-on-account-delete, chat_logs SET NULL, append-only consent, and per-account read isolation. Each of the twelve assertions was mutation-tested by breaking the policy or constraint it guards and confirming the test fails.
- Shim moved to supabase/tests/000_supabase_shim.sql, shared by CI and the new scripts/schema-check.sh so a schema failure reproduces locally; auth.uid() now accepts both JWT claim GUCs like Supabase's own, since a shim easier to satisfy than production makes CI a weaker gate than it looks. 530 tests green.

## 2026-08-04 — PLAN.md Part 5 checkpoint 4: the banner now reaches every page
- Checkpoint 4 requires the "not an official Government of AP website" line in the footer of every page. It was missing from two whole classes of page: any unmatched URL and any server error both render outside every layout, so Next's bare English-only 404 and "Application error" screens carried no footer, no nav and no banner — on a Telugu-first site distributed by WhatsApp link, where stale links are routine.
- Fixed with a [locale]/[...rest] catch-all that turns an unmatched URL into a notFound() inside the locale layout, plus [locale]/not-found.tsx and [locale]/error.tsx, both bilingual since neither boundary receives route params. A not-found.tsx alone does nothing here — that boundary is only reached when a matched route calls notFound(), which is what the catch-all is for.
- Verified in a real browser over 28 routes reading laid-out text, not HTML source: the RSC payload embeds the banner string in a <script> tag, so grepping the response body reports it present on pages where it never rendered. scripts/verify-footer.mjs (pnpm verify:footer) keeps that reproducible and also fails a soft 404. Structural guard added for the two routing facts that make the invariant hold; both mutation-tested. 532 tests green.

## 2026-08-04 — PLAN.md Part 7 closed out; unverified-rate warning verified on screen
- Checked the four open decisions against the code rather than against memory. All settled: 13 calculators (Part 4, TASKS.md, 13 route dirs and 13 registry entries agree), embeddings pinned at 1024 with `dimensions: 1024` sent to OpenAI and a width guard on both providers, Surya left a typed stub with Gemini vision as the working fallback so no hosting choice is owed yet.
- The one decision still needing a human — the scraper contact address — is enforced, not trusted: buildUserAgent throws on unset, on placeholder patterns (example.com, <…>, todo, changeme) and on anything that is not an address, so no crawl can start without one. Part 7 now records status instead of asking questions the code already answered.
- Verified in a browser that the unverified-rate warning actually reaches the screen, since shipping seeded-but-unchecked rates rests entirely on it: DA arrears for the worked example returns ₹40,194 citing G.O.Ms.No.60, with the amber "not yet checked against the GO" notice directly above the WhatsApp-share and print buttons — so the caveat travels with the figure. 532 tests green.

## 2026-08-04 — security: both bot webhooks were fail-open on a missing secret
- Reviewed the pre-launch attack surface (public webhooks, admin gate, service-role key, XSS sinks, SSRF in media download). One real defect, in both bots: the WhatsApp route verified the signature only `if (appSecret)`, and the Telegram route passed grammY's secretToken only when the env var was set. grammY's own source says it plainly — "if no token is configured, accept all requests" — so an unset variable turned an authenticated endpoint into an open one, invisibly, because real traffic keeps working.
- Reachable without the secret: queue attacker-supplied PDFs into ingest_queue, spend Gemini budget on forged questions, and make our own WhatsApp number send text to any recipient the attacker names. Rule 6 still gates the corpus behind admin approval, so this was abuse and cost, not corpus poisoning.
- Both now fail closed via requiredWebhookSecret, matching the convention the scraper already follows (CLAUDE.md rule 3 refuses to crawl without a contact address). Seven tests drive the route handlers rather than the verifying helper, since the helper was never wrong — the call sites were; mutation-tested by restoring the old form, which answered 200 to an arbitrarily-signed POST. Admin gate, GET handshake, service-role key handling and the media path were checked and are correct. 539 tests green.

## 2026-08-04 — bot webhooks: per-sender throttling
- Closes the gap left by the fail-open fix: with the caller authenticated, the remaining vector is a real sender behaving badly, and the bots did no limiting at all. Keyed on the sender (WhatsApp phone number, Telegram from.id), never on the request — every webhook arrives from Meta's or Telegram's servers, so an IP key would put every user in one bucket and let one spammer silence the bot for everybody.
- Two budgets, because they cost different things: questions get the web chat's 20 per 5 minutes, forwarded documents 15 per hour, since what an upload consumes is an administrator's review attention and that does not replenish on a five-minute timer. Set generously — somebody forwarding a batch of GOs they collected is the best thing that can happen to this corpus.
- Refusals say why once, then go quiet: replying to every refused message would make our own number the amplifier, a thousand messages in and a thousand paid replies out. The notice is bilingual. Telegram runs it as middleware ahead of every handler, exempting channel posts (the allow-list is the limit there). Seven tests cover the throttle decisions; the route wiring itself is covered by typecheck and review only, since handleMessages cannot be exported from a Next route module. 546 tests green.

## 2026-08-04 — error tracking wired; selector verifier now fails usefully
- Ran verify:selectors to check it before handing it over. The politeness logic is right (it refuses to crawl when robots.txt cannot be read) but the refusal surfaced as an unhandled rejection with a raw stack trace, and it could not tell "your network cannot reach the site" from "the site is blocking this bot" — opposite responses. Added a tested diagnose() covering unreachable, 401/403, 429, 5xx, missing Playwright browser and missing contact email; the 403 case explicitly says not to disguise the User-Agent but to contact the site with the address in it.
- Sentry wired and inert without a DSN. Caught a real cost while measuring: a static client import added 83 kB to First Load JS on every page — on a mobile-first site with a Lighthouse ≥90 target, for a feature nobody had configured. Made it a dynamic import behind the DSN check; First Load JS is 103 kB with or without a DSN, and with one the SDK loads as a lazy chunk after hydration.
- Scrubbing is deny-by-default and is where the real work went: request body, cookies and non-describing headers dropped entirely, query parameters allow-listed, inferred user removed, long digit runs masked in breadcrumbs so a phone number or basic pay cannot ride along while "G.O.Ms.No.60, HTTP 500" stays readable. Tracing is off and Session Replay is not enabled — it would record somebody typing their pay into a calculator. /privacy now discloses the service in both languages, since wiring a processor without saying so is the DPDP failure. 563 tests green.

## 2026-08-04 — the GO scraper no longer depends on selectors nobody has verified
- selectors.ts pins columns positionally (td:nth-child(2) is the department) and was written without ever loading goir.ap.gov.in. Added inference that reads the columns from content instead — a GO number looks like one, a date parses, the subject is the long free-text field, the link is an anchor — so the first crawl can work on markup nobody has inspected. Positional selectors stay the fast path; inference runs when they fail, and prints the mapping it found so correcting them is one edit.
- Testing it end-to-end in a real browser found a worse bug than the one designed for. Against a listing with a leading serial column, the configured selectors matched 3 rows — the wrong cells. goNumber was "1", department held "G.O.Ms.No.60", and parseIndexRows accepted all three with zero skipped, because it deliberately keeps unrecognised text for human review. A full crawl would have completed, reported success, and filed every order under its row number.
- So the fallback triggers on row quality, not row count: scoreRows checks whether the cells hold what their names claim, weighted hardest on the GO number. The slip scores 0.50 against a floor of 0.70, inference recovers it at 1.00, and inference is never allowed to replace better rows with worse. 18 tests over reordered columns, absent headers, serial columns, Telugu headers and link-on-number. 581 tests green.

## 2026-08-04 — setup made followable without technical knowledge
- The owner is not a developer, and every remaining step had been phrased as technical homework. Added `pnpm setup:check`, which reports the portal stage by stage — calculators need nothing, the GO library needs a database, chat needs a model key — so "not everything is configured" reads as "here is what works today", not as failure. It names each missing variable in plain words with where to get it.
- It checks the thing, not the setting: it calls the Supabase REST endpoint and distinguishes connected-with-tables, connected-but-tables-missing, key-rejected, unreachable and paused, each with the one action that fixes it. Fixed a flaw found while testing — with every variable filled but the database unreachable it announced "everything is configured", which is technically true and practically wrong, since the portal would serve no GOs at all.
- SETUP.md walks from zero to a public site: create the Supabase project, run the six migrations in order, see it locally, deploy to Vercel, then fill the library by hand or by crawl. It leads with the fact that a first launch of calculators plus GO library is worth shipping, and repeats the unverified-rates warning where it will actually be read. 581 tests green.

## 2026-08-04 — the missing link: approved GOs were never made searchable
- Asked to bulk-load GOs, went looking for the bulk path and found a worse problem: extract, chunk, embed and write-chunks each existed and were each tested, and nothing called them in sequence. Approving a Government Order inserted a documents row and stopped. The GO appeared in the library and the chat could never find it — no chunk of it existed to retrieve — so the answer was "not found in corpus" forever, silently, no matter how many were approved. Phase 1 item 4 was marked done on the strength of the parts existing; the seam was never built.
- Added packages/ingest/src/pipeline/index-document.ts joining the four, plus `index:documents` to run it over every approved-but-unindexed document. The absence of chunks IS the queue: no extra state column, self-healing after a crash, safe to re-run. Refuses to index a scan's whitespace residue (a document that retrieves for nothing and can be cited for nothing is worse than an absent one), embeds once per document rather than per chunk since providers bill per request, and replaces chunks on re-index rather than duplicating.
- Wired into the nightly workflow so approval is the only human step. Six tests drive a real generated GO PDF through the whole chain and assert on the seam: vectors on every written row, GO number carried into what gets embedded so answers can cite, one embed call per document, scans left for OCR. 587 tests green.

## 2026-08-04 — web search reachable, fetching is not; a DA arrears caveat found
- Retested network access properly instead of assuming: the WebFetch tool is blocked for every host (example.com returns the same 403 as goir.ap.gov.in, so it is the environment, not the sites), but web SEARCH works. So the sources can be located and not downloaded.
- Declined to build the corpus from search results. What comes back is blog summaries — jnanaloka, apteachers and the like — and CLAUDE.md rule 3 says official PDFs only, rule 2 says retrieval-grounded from those PDFs. Populating the library from third-hand summaries would reproduce the unverified-rates problem at corpus scale, except invisibly, because a GO in the library reads as authoritative in a way a flagged rate does not.
- Search did corroborate the seeded DA figure (33.67 → 37.31 w.e.f. 01-01-2024, G.O.Ms.No.60) from several independent sources, and surfaced something the calculators did not say: those arrears may be payable only at exit from service rather than now. Added a payout-timing note to the DA arrears result — phrased as "check the GO for when this is paid", not as a claim, since the source is secondhand. The total is the figure people forward to each other, and a number with no timing reads as money arriving shortly. 587 tests green.

## 2026-08-04 — a starter list of real GO PDFs, and a collector that fetches it
- Search works here even though fetching does not, so used search to find the addresses of real Government Order PDFs and built config/go-sources.json: fifteen documents across DA, the 2022 pay revision, pensions and allowances, several on official domains (apfinance.gov.in, cag.gov.in) rather than only union mirrors. Also recorded three index pages worth crawling later, including AP Finance's own 11th PRC page.
- The `claim` field in that file is explicitly not data. It is what a search engine said, recorded so a reviewer knows what to expect when the PDF opens; every field that reaches the library is read from the PDF by the extractor, and an admin approves it. Nothing was written into the corpus from search results — a fake GO reads as authoritative in a way a flagged rate does not.
- `fetch:seeds` downloads the list through the existing politeness, hashing, dedupe and queue machinery, so the seed route and the crawl route end in the same review queue. Testing caught two things: Node's fetch does not implement file: URLs, so reading the config failed as an opaque "fetch failed"; and a failed robots.txt was not cached, so one unreachable host was asked once per document listed against it — eight retries at a site that is down is exactly what rule 3 exists to prevent. Both fixed. 587 tests green.

## 2026-08-04 — the front page was still the Phase 0 placeholder
- Captured every page in a browser to show the owner what had actually been built, and the first screenshot showed the problem: the home page carried a hardcoded "coming soon" badge on all three launch-wedge cards, and none of them were links. It was written in Phase 0 and never revisited, so the first thing any visitor saw advertised a finished portal as unavailable — including DA arrears, which has worked for days.
- Cards are now links with per-card status: DA arrears reads "available now" in green, GO search and chat read "once GOs are added", which is true rather than flattering — both pages work, and both are empty until documents are approved. Added a route to all 13 calculators and a line saying they need no account and send nothing anywhere, since that is the part which works for a visitor today with no setup at all.
- Fifteen pages captured across both languages at mobile width; all render, the 404 returns a real 404, and every page carries the not-official-government footer. 587 tests green.

## 2026-08-04 — eleven of the thirteen calculators were showing English to Telugu readers
- Exercised all 13 in a browser rather than the 4 sampled for the tour. Every one produces a result with no JS errors — but the labels told a different story: "Taxable income", "Monthly pension", "Retirement date", "Total service (years)" rendered on the Telugu page. 55 hardcoded English strings across eleven calculators; only DA arrears and salary had ever been localised. For a portal whose whole premise is Telugu-first, that is the promise broken on the pages people actually came for.
- Localised all 76 distinct labels into calculators.labels, matching the register the existing Telugu already used — transliterated loanwords for service terms (బేసిక్ పే, జీపీఎఫ్, ఏపీజీఎల్‌ఐ) and Telugu for the rest, which is how AP employees actually speak rather than how a dictionary would render it.
- The salary breakdown needed a design change, not a translation: packages/calc was emitting display strings ("DA @ 37.31%"), so a pure calculation package was deciding what language a figure is read in. It now returns a language-neutral key plus the percent, and the UI composes "డీఏ @ 37.31%" — the rate survives translation instead of being frozen into an English string. Verified label-by-label across all 13 in both languages; also deleted the coming-soon component, which nothing had rendered since Phase 0. 587 tests green.

## 2026-08-04 — the database-backed pages seen rendering for the first time
- The GO library, news feed and per-GO pages had only ever been unit-tested; nobody had watched them render, because the real PostgREST is a Haskell binary this sandbox cannot download. supabase-js speaks plain HTTP, so scripts/fake-supabase.mjs answers those requests directly — enough of PostgREST's filter syntax to drive the pages, with every row labelled SAMPLE so no screenshot could be mistaken for a loaded corpus.
- Both states check out. Empty: the library says "still empty — GOs will appear here once approved" and news says "no news yet", which is what the owner sees the minute they deploy and is the difference between a working site and an apparently broken one. Populated: three orders list correctly with Telugu subjects, the department filter fills itself from the data, and the superseded order carries its amber "this GO has been cancelled" badge — the per-GO page draws the chain from No.28 to No.60.
- One more localisation gap found the same way: the news filter chips read Transfers / Exams / Pension / Leave in English. The tag is the URL and the value stored against a document, so it stays English; only its label is translated (బదిలీలు, పరీక్షలు, పెన్షన్, సెలవులు). 587 tests green.

## 2026-08-04 — the review queue, and a stranger's forward that looked official
- Walked the admin queue against the stand-in: gated before sign-in with no rows leaking, a wrong password refused, the right one showing the queue with source and arrival time per document, and "no document reaches RAG until approved" stated at the top of the page. The page a reviewer will live in, seen working for the first time.
- Looking at it found the defect. A PDF forwarded to the bots by any member of the public carried an explanatory note in its metadata but no needs_review flag — and the queue lists documents by that flag, so the least trustworthy input in the whole system rendered identically to one the scraper fetched from goir.ap.gov.in and the extractor read confidently. The warning was sitting in the data precisely where the reviewer could not see it. Anyone can forward anything to a WhatsApp number.
- Forwarded documents are now flagged with the reason recorded, so the amber "review needed" badge marks them in the list. Test extended to assert the flag rather than only the note, since the note was never what the queue reads. 587 tests green.

## 2026-08-04 — the chat answering, end to end, without keys
- Last unseen surface. Both API base URLs were hardcoded, so nothing could stand in for them; they are now env-overridable (OPENAI_BASE_URL, GEMINI_BASE_URL), defaulting to the real services. That is worth having beyond testing — a deployment behind a proxy or on a regional endpoint no longer needs a code change.
- scripts/fake-model-apis.mjs answers in the real shapes: OpenAI's embeddings payload, Gemini's SSE stream, deliberately spaced so streaming is visibly streaming. Verified the whole path: /api/chat emits the citations frame BEFORE any token — the ordering chosen so a reader sees the source before the prose — then tokens; the UI grows its answer while streaming (655 → 814 characters mid-flight), renders the citation card with GO number, date and PDF link, keeps the bilingual disclaimer inside the answer, and shows the feedback buttons.
- The buttons were missing on the first run and it was not a bug: they render only when a chat_logs row exists to attach a rating to, and the stand-in was not handling inserts. Feedback with nowhere to go should not be offered. Completed the stand-in and they appeared. 587 tests green.

## 2026-08-04 — the ingestion chain run end to end, and a bug only that could find
- Every piece had been verified alone; the chain never had. Served a generated GO PDF over a local host with its own robots.txt, ran the real fetch:seeds against it (robots honoured including Crawl-delay, PDF confirmed by magic bytes, hashed, queued), then ran index:documents for the first time — that CLI had only ever had its inner function unit-tested, and CLIs are where the wiring bugs live.
- It found one. OPENAI_BASE_URL was honoured by the chat's embedder but not by the ingest package's, so the indexer still called api.openai.com. Two embedding call sites, one made overridable — the kind of half-applied change that unit tests cannot see because each site is correct in isolation. Both now honour it, and DEEPINFRA_BASE_URL for the same reason.
- Then the stand-in itself was wrong: it returned one unindexed vector where the real API returns one item per input carrying its index, and the embedder correctly refused it. Worth the fidelity — a stand-in looser than the real service passes things production would reject. Fixed, and the chain completes: PDF in, one searchable chunk out, the two fixtures with no file on disk reported rather than silently skipped. 587 tests green.

## 2026-08-04 — the golden-set eval had never run, and could never have passed
- Ran `pnpm eval` for the first time, against the stand-ins. It crashed before asking a single question: the golden set is at the repository root and the path was resolved against the working directory, which for `pnpm eval` is packages/rag. The documented command and the CI job both use that invocation, so the gate would have failed the moment the secrets were set — the score functions were unit-tested, the runner never executed. Path is now resolved from the module.
- Then the real design bug. The golden set is 30 cases and /api/chat allows 20 per five minutes, so the eval was rate-limited by its own site: everything past the twentieth failed as "request failed" and the cite rate could never clear 90%. A gate that cannot pass teaches people to ignore it, and this one would have looked like a broken model rather than a working rate limiter.
- Fixed by waiting rather than by exemption: on 429 the runner reads the endpoint's own Retry-After and sleeps, up to five times per case. A header that skipped the limit would be a bypass anyone could send, and the eval exists to exercise the endpoint real users get. Confirmed live — it read "retry-after: 212" and waited. 587 tests green.

## 2026-08-04 — checked whether the remaining work can be delegated to another AI
- Tested rather than assumed: spawned a separate Claude session in a separate container and had it curl the six source hosts. All returned 000. The block is the environment's network policy, not this session, so handing the collection work to another agent changes nothing. Gemini is also not a tool available here — it is the model the product itself calls once a key exists.
- The rest is not blocked by capability but by authority. A Supabase project, a Google AI key and a Vercel deploy need the owner's identity, billing and agreement to terms; no assistant can open accounts in someone's name. And the rate check needs a person who is accountable for the figure — a second model reading a GO and pronouncing it correct is the same act as the first model reading a web summary, which is precisely how these rows became unverified.
- So the useful move was to shrink the irreducible job rather than hand it back. `pnpm rates:worksheet` prints all 16 rows as a checklist ordered by how much of the site each feeds — DA first, since it reaches all 13 calculators — showing the current value, the claimed GO where one is known, and which have no GO identified at all. 0/16 confirmed today. --csv for a spreadsheet.

## 2026-08-07 — the seed migration said "regenerate", and no generator existed
- Migration 005's header instructs anyone changing a rate to edit config/rates/rps-2022.json and regenerate, never to hand-edit the SQL — but no generator had ever been written, so the only way to apply a verified rate was to hand-edit the file that forbids hand-editing. The JSON is bundled into the browser for the calculators; the SQL seeds the database the server reads. Sixteen rates are queued for verification, which is sixteen chances for the figure on the page and the figure in the database to disagree about what an employee is owed, silently.
- scripts/generate-rates-seed.mjs now writes migration 005 from the JSON, and `--check` fails when the two drift. Mutation-tested in all four directions before trusting it: JSON edited without regenerating, a rate value hand-edited in the SQL, an _unverified flag hand-flipped to false, a whole row deleted — each caught, and clean again once reverted. Wired into ./scripts/gate.sh and CI's verify job, since a guard nobody runs is not a guard.
- Also recorded what research could not settle: two searches place G.O.Ms.No.28 at DA 26.39%→30.03% w.e.f. 01-01-2023 and an earlier one at 22.75%→26.39% w.e.f. 01-07-2022. Both go-sources.json and the rate row now say DISPUTED with both readings rather than picking one, so the reviewer opening the PDF knows what to settle. Still 0/16 verified — a candidate GO number is not a confirmation. Full gate green; migration applies to a real Postgres from nothing.

## 2026-08-07 — the site run with no configuration at all, and what a reader was told when it broke
- Started the production build with an empty environment — the state the owner's first deploy is one typo away from — and walked every page in a browser. It holds up better than expected: all 13 calculators load and compute client-side, /gos says "database not connected yet", /news says "no news yet", /dashboard offers sign-in. So the calculators can go live on their own, before any Supabase project or model key exists.
- The chat could not. Asking "డీఏ ఎంత శాతం?" put **"NEXT_PUBLIC_SUPABASE_URL is not set"** in the answer bubble — an English environment-variable name presented to a Telugu-speaking employee as the answer to their question. The route was careful ("must not be reported as an answer") but the client rendered the body's error field verbatim for every failure, so a 429 read "Too many requests" and a long question read "Question is longer than 2000 characters". Rule 5 broken on the most visible AI surface, and a map of our wiring handed to anyone who could break it.
- Now the status code picks the message and the dictionary supplies it; server prose never reaches the page and goes to the log instead, where it is useful. A 429 names the real wait from Retry-After ("5 నిమిషాల తర్వాత"), and an over-long question is caught before it leaves the browser. The same defect was on the bots: /da returned any error's message, including RateNotFoundError's "seed or verify the row before calculating" — advice for the maintainer, sent to a stranger. An existing test caught me over-narrowing that catch and swallowing a real user-facing refusal, which is exactly what it was there for. 611 tests green.

## 2026-08-07 — the same defect on three more surfaces, one of them the launch-critical one
- Having fixed the chat, swept for every other place a server or library error reaches a reader. Three more, and the worst was the flagship calculator: entering a month before the seeded DA timeline told a Telugu-speaking employee, in English, *"No DA rate is on record for 2017-01-01. Rates come from the rates table (CLAUDE.md rule 1); seed or verify the row before calculating."* That is on the one surface deployable today. My first selector missed it because the class is text-red-800, not text-red-700 — the page said it, my check did not see it.
- The refusal is right: the calculator declines rather than inventing a figure. Only the words were wrong, and they are what a maintainer reads in a failing test. So every calc error now carries a `code` and its particulars while keeping its English message for tests, and the UI picks a sentence by code and fills in the specifics. The reader keeps "2017-01-01" and loses the instructions meant for someone else. isCalcError is structural, not instanceof, because the web app and the calc package are separate builds and instanceof across that boundary fails silently.
- Also: the account page rendered ?error= straight from the URL, so Postgres's "new row violates row-level security policy for table users" — and anything else anyone put in the query string — appeared on the page; codes travel now and an unknown one falls back to the generic message. And the quiz echoed the route's error the same way, so a rate-limited reader saw "Generation failed" instead of being told to wait. 630 tests green, all four surfaces confirmed in a browser.

## 2026-08-07 — the plan's own pre-launch checklist, made runnable
- With every phase task checked, the next thing the plan actually asks for is PART 5 checkpoint 4: the legal checklist before public launch. Three of its four items had never been verified — only the footer banner had a script. So `pnpm verify:launch <url>` now runs all four: disclaimer and privacy pages live and substantive in both locales (a stub still returns 200, so it asserts length and that the Telugu page is in Telugu), DPDP consent and deletion, no third-party requests, and the banner via the existing footer check.
- The strongest of these is the one a source grep cannot do: load thirteen pages in a browser and assert that not one of them contacts any host but its own. That is what "unmonetized" and DPDP actually mean at runtime — every third party that sees a visitor's IP is a disclosure — and a font import or an embedded widget would show up there and nowhere else. Sentry is the one deliberate exception and only with a DSN set; the check runs against a build without one. It passes on 13 pages today.
- Mutation-tested rather than trusted, because a checklist that has only ever passed proves nothing: an injected ad tag was caught on all 13 pages, a removed consent checkbox and a deletion with its typed confirmation disabled each failed it, and all three exited 1 while the restored tree exits 0. The output also states which items are runtime and which are read from source — the signed-in consent flow needs a real Supabase session, and saying so is the difference between a checklist and a decoration.

## 2026-08-07 — checked the arrears arithmetic against the plan, and the plan was wrong
- With the plan's remaining checkpoints all needing a corpus, a key or a person, the useful move was to verify what is already shipped. Recomputed DA arrears independently from the rate table — deliberately not importing the calc package, so a misunderstanding shared between the calculator and its own tests could not pass both. Basic ₹52,590, Jan–Jun 2024, paid 30.03%: my figure ₹22,971, the page's ₹22,968.
- The page is right and PLAN.md was wrong. Part 4 wrote the arrear as basic × (newDA − oldDA)/100. But DA appears on a pay bill as a whole-rupee figure, so what an employee is owed is the gap between what the bill should have said and what it did say — round(basic × newDA%) − round(basic × oldDA%). The two rules disagree by a rupee a month on about a quarter of the master scale's stages. The code already modelled the bill, with a comment saying so; the plan carried the shorthand.
- Worse, no test could tell them apart: every worked example in the suite happens to agree under both rules, so a rewrite to the shorthand would have passed everything and quietly changed what the site tells people they are owed. Added a case at basic ₹21,200 where the shorthand gives 1,543 and the pay bill 1,544, and mutation-tested it — switching the code to the shorthand fails that test alone, the other 14 pass. PLAN.md Part 4 now states the real rule.
- Also audited the plan's feature list against the build and found one genuine gap: the AAS 6-12-18-24 scheme is not encoded. The FR 22-B mechanism is there, but not which service lengths earn SG/SPP-I/SAPP-I/SPP-II, so the fixation calculator only helps someone who already knows their promotion scale minimum. Recorded under BLOCKED rather than filled in from memory — rule 1 forbids a rate with no source_go and rule 2 forbids answering AP rules from memory, and plausible-looking service lengths are precisely what those rules exist to stop.

## 2026-08-07 — three calculators showed numbers that did not add up
- Carried the DA audit's question to the rest of the suite: does what is on screen actually multiply out? Leave encashment showed "per day ₹2,407 × 300 days = ₹7,22,113" — ₹13 adrift. Gratuity showed emoluments ₹72,211 where ₹72,211 / 2 × 33 half-months falls ₹5.50 short of the total beside it. Both totals were right; both displayed a rounded intermediate that the total had not been computed from, at the moment someone is checking a retirement payout most closely.
- The codebase had already settled this principle and applied it in one place: projectNps rounds corpus and contributions once so "corpus − contributed" equals the growth on screen, with a comment about not inviting a support query about arithmetic that is in fact correct. So this was applying an existing rule consistently rather than inventing one. Both functions now return exact figures and the pages render them with paise. No payout changed.
- Paise alone did not rescue leave encashment: over 300 days a rounding error in the second decimal is still a rupee, so a per-day rate cannot be written at currency precision and also multiply out. The page now shows monthly emoluments — the figure the rule is actually expressed in, emoluments × days / 30 — which lands exactly. Both reconcile in a browser now.
- The tests could not have caught any of this: every existing case used a basic pay of ₹1,00,000, whose emoluments and daily rate are both whole rupees. Added cases at ₹52,590 where they are not, and confirmed by mutation that restoring the rounding fails exactly those two. Also filed what I did not decide: whether a payout GO rounds monthly emoluments before applying the formula is a rules question, and DA arrears and gratuity currently answer it differently. 633 tests green.

## 2026-08-07 — swept all thirteen calculators, and made the decisions rather than asking
- Drove every calculator in a browser with a basic pay whose DA-inclusive emoluments are deliberately fractional. Twelve held up; increment and fixation correctly refuse a pay that is not a master-scale stage, which is the error path fixed earlier working as intended. The salary calculator did not: its WhatsApp share quoted "Gross ₹80,625", a figure that appeared nowhere on the page, because a row of em-dashes stood where the gross line belongs. A reader forwarding that to colleagues was passing on a number they had never been shown and could not check.
- Fixed by giving it the order a pay slip is read in — earnings, gross, deductions, net — and wrote `pnpm verify:calculators` to stop it coming back: every rupee figure in a share text must be visible on the page, across all thirteen. The first version of that guard was worthless and I only found out by trying to break it: with the deduction fields at their default of "0", gross equals net, so the missing row was invisible and the check passed a build that had the defect. It now forces every amount field non-zero, and the mutation is caught.
- Decided the rounding question rather than leaving it filed. DA arrears and gratuity differ on purpose: DA arrears subtracts two amounts that were really paid, and DA appears on a monthly bill as whole rupees, so both sides round before subtracting. Gratuity is not a difference of past payments — it is one payment computed once from a notional figure, so it rounds once, at the end. Rounding the intermediate too would round twice for no reason. Removed from BLOCKED, reasoning left in the code.
- AAS stays unencoded — inventing service-year thresholds is exactly what rules 1 and 2 forbid — but the fixation page no longer leaves a reader to discover that. The promotion-scale field now says, in Telugu and English, what to enter and that the AAS thresholds are not on record, so it reads as a calculator honest about its limits rather than a broken one. 633 tests green.

## 2026-08-07 — looked at the site on a phone for the first time
- Every check so far had run at desktop width, and this portal's whole distribution model is a WhatsApp link opened on a budget Android. Drove every page and five calculators-with-results at 360px. The layout held — nothing scrolls sideways, including the month-by-month arrears table, which only exists after submitting and so had never been rendered by any earlier check.
- Tap targets did not hold. The privacy and disclaimer links in the footer measured 20px tall against WCAG 2.2's 24px minimum — on every page of the site, and they are precisely the two links a DPDP notice depends on being reachable. The "all 13 calculators" link on the home page was 19px. All three now clear the minimum via inline-block padding; visual size is unchanged.
- `pnpm verify:mobile` keeps both properties: no sideways scroll and no sub-24px target, checked at 360px on every page and on calculators with results on screen. Mutation-tested both halves — stripping the footer padding is caught (the exact original bug), and a forced 900px element is caught. One lint round: the page.evaluate callback runs inside Chromium, so `document` is declared as a global for that file rather than contorting the code. 633 tests green.

## 2026-08-08 — what the site tells crawlers, and where the bots send people
- Checked the surfaces a person never looks at: robots.txt and sitemap.xml on the zero-config build. The sitemap handed Google 52 http://localhost:3000 URLs and robots pointed crawlers at a localhost sitemap — every page renders fine, so a deploy that forgot NEXT_PUBLIC_SITE_URL would simply never be indexed and nothing would look wrong. Worse: both bots' replies fell back to https://ap-emp-ai.in, a domain nobody on this project is known to hold; if anyone registers it, every Telegram and WhatsApp reply directs employees to a stranger's server.
- One resolver now answers "what is this site's address": configured value, else Vercel's production hostname, else null — and null means degrade honestly. robots.txt drops its Sitemap line, the sitemap is empty, the bots omit their link line. A missing link is a nuisance; a wrong one is a trap. Proved both paths against real builds, which surfaced an operational fact now documented in .env.example: NEXT_PUBLIC_* is baked in at build time, so setting it at `next start` does nothing — it must be set where the build runs.
- verify-launch grew a crawler section (no localhost in robots or sitemap, /api/ still disallowed), mutation-tested: restoring the localhost fallback fails it with "52 localhost URL(s)". Also fixed a straggler found in passing: the bots' embedder still hardcoded api.openai.com after the OPENAI_BASE_URL override was applied to chat and ingest — third call site, now consistent. 633 tests green.

## 2026-08-08 — the printout, and what a first visit costs in data
- Pressed the Print/PDF button that every calculator has carried since Phase 3 — apparently for the first time. The printed page was close to right (form hidden, table, source GO, warnings, disclaimer all present) but had the same defect the share text had: a total with no premises. ₹80,388 over 21 months, and nowhere on the paper the basic pay or paid-DA it came from. Handed to a DDO or a colleague, it cannot be checked. The nav row and the English toggle also printed — dead links and a dead button on paper.
- The result panel now carries a print-only block listing every input by its localized label, generically for all 13 calculators, and the header's chrome is print-hidden while the brand line stays (a printout should say where it came from). Verified by rendered boxes, not computed style — a child of a display:none parent keeps its own computed display, which my first probe got wrong. verify:calculators grew a print section, mutation-tested: unhiding the block's print style fails exactly the two input assertions.
- Also measured what the site costs on metered data, since the entire audience is on it: 359 kB on the wire for a first visit, ~40 kB a page after. Half the first visit is the Telugu font — one variable file, already cheaper than static weights, and the irreducible price of no tofu. Set a 450 kB budget in verify:mobile with headroom over today: it exists to catch an accidental second font family or a dependency landing in the shared bundle, not to start a diet. 633 tests green.

## 2026-08-08 — the quiz generated questions for the first time
- The practice-quiz generator had never once produced a question anywhere: the route calls Gemini's non-streaming :generateContent, the chat honours GEMINI_BASE_URL but the quiz had its base hardcoded, and the stand-in only implemented the streaming endpoint. Three small gaps that together meant the feature's success path was unreachable in any environment without a paid key. Only its error path had ever been seen.
- The route now honours GEMINI_BASE_URL with the same convention as the chat (one value redirects both call sites), and the stand-in answers generateContent in the real API's shape — deliberately including one question with three options and one with no source, so parseQuiz's drop-don't-repair rule is exercised on every run rather than only asserted in unit tests. The route order matters and is commented: "generateContent" is a substring of "streamGenerateContent", so the streaming route is matched first.
- Driven end to end in a browser against both stand-ins: generate → exactly 2 valid questions render (both malformed ones dropped, nothing leaked), reveal → correct answer highlighted, explanation shown, and each question carries its source GO with date — rule 2's chain intact from retrieval to screen. Chat re-verified over the same GEMINI_BASE_URL to prove one value now drives both. 633 tests green.

## 2026-08-08 — the language toggle dropped whatever was in the URL
- Checked three daily-use behaviours never once exercised: the language toggle, the dashboard's prefill links, and keyboard-only operation. The toggle kept the page on all five routes tried — but dropped the query string, so a dashboard-prefilled calculator link (?basicPay=52590) lost its numbers the moment the reader pressed English. Fixed: the query rides along, and the toggle gained the Suspense wrapper useSearchParams demands on static pages.
- Two of my own probes were wrong before the product was. "Enter does not submit" turned out to be Chromium's month input consuming multiple Tab presses — my third Tab never left fromMonth; Enter submits fine from a number field, focus rings are visible on everything tabbed, and ?basicPay= prefills correctly. And my first mutation test of the new guard was invalid twice over: the mutation left `search` unused, noUnusedLocals failed the build, the guard's exit 1 came from navigating to a server that never started. A type-clean mutation (void query) finally failed the one assertion it should — "toggle keeps the query string" — and nothing else.
- verify:calculators grew the prefill journey: parameter prefills the field, toggle keeps the page, toggle keeps the query. 633 tests green.
